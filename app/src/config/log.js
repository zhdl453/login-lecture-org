// const fs = require("fs");
// const appRoot = require("app-root-path"); //프로젝트 루트를 찾아주는 app-root-path

// const accessLogStream = fs.createWriteStream(
//     `${appRoot}/log/access.log`,
//     { flags:'a'}
// );

// module.exports = accessLogStream;